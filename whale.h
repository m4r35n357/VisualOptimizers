
#include "base.h"

whale *create_whale (int dim, real min_x, real max_x);

bool woa (point *solution, int max_i, int n, int dim, real min_x, real max_x);

real cost (real *x, int dim);

int randint (int n);

real randreal (void);
