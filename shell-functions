
random() {
dim=$1
lower=$2
upper=$3
string=""
count=1
while [ $count -le $dim ]
do
    string="$string $((lower+$RANDOM%(upper-lower))).$(($RANDOM%999))"
    count=$((count + 1))
done
echo $string
}

